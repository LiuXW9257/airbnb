import styled from "styled-components";

// 相当于创建HeaderWrapper标签
export const HeaderWrapper = styled.div`
  display: flex;
  /* 副轴方向居中 */
  align-items: center;
  height: 80px;
  border-bottom: 1px solid #eee;
`;
